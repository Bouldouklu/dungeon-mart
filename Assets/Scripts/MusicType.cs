/// <summary>
/// Defines all background music tracks in the game.
/// Used by AudioManager to play phase-specific music.
/// </summary>
public enum MusicType {
    MorningMusic,      // Calm preparation music (Morning phase)
    BusinessMusic,     // Upbeat shop music (OpenForBusiness phase)
    EveningMusic       // Relaxed closing music (EndOfDay phase)
}
